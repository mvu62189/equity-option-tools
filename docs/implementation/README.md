# Implementation Records

Status: current build records for shipped features.

This tree captures how features were implemented, why certain tradeoffs were chosen, and what follow-up work remains after the first shipped version.

- Use top-level `docs/*.md` for canonical current behavior.
- Use `docs/implementation/**` for implementation notes, rollout details, and feature-specific design records.
- Use `docs/historical/**` for archived planning material that is not guaranteed to match the code.

Current implementation records:

- `spy-short-expiry-workstation/`
- `batch-scoped-review-ssvi-trace/`
